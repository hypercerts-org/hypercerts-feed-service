{
  "lexicon": 1,
  "id": "org.hypercerts.feed.getFeed",
  "defs": {
    "main": {
      "type": "procedure",
      "description": "Builds an ordered hydrated feed from current indexed records and identities for the registered feed and parameters selected in the request. Authentication is optional. When supplied, the AT Protocol service-auth issuer is the viewer and may omit params.viewerDid; anonymous requests must provide it.",
      "errors": [
        { "name": "InvalidRequest", "description": "The request or the selected feed's parameters are malformed or semantically invalid." },
        { "name": "UnsupportedFeed", "description": "The requested feed identifier is not registered by this service." },
        { "name": "InvalidCursor", "description": "The cursor is invalid or incompatible with the selected feed." },
        { "name": "InternalError", "description": "The service encountered an internal error." }
      ],
      "input": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["feedId"],
          "properties": {
            "feedId": {
              "type": "string",
              "maxLength": 512,
              "knownValues": ["org.hypercerts.feed.defs#hypercertsFeed"],
              "description": "Identifier of the feed algorithm to execute."
            },
            "params": {
              "type": "union",
              "closed": false,
              "refs": ["org.hypercerts.feed.defs#hypercertsFeedParams"],
              "description": "Algorithm-specific parameters. Omit when the selected feed accepts none."
            },
            "limit": {
              "type": "integer",
              "minimum": 1,
              "maximum": 100,
              "description": "Maximum number of feed subjects to return. The selected feed determines the default and may enforce a lower maximum."
            },
            "cursor": {
              "type": "string",
              "maxLength": 4096,
              "description": "Opaque cursor returned by a previous request for this feed."
            }
          }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "description": "Returns ordered hydrated feed entries and an optional cursor for the next page.",
          "type": "object",
          "required": ["feed"],
          "properties": {
            "feed": {
              "type": "array",
              "items": {
                "type": "ref",
                "ref": "#feedItem"
              }
            },
            "cursor": {
              "type": "string",
              "maxLength": 4096,
              "description": "Cursor for the next page. Omitted when no later page is known."
            }
          }
        }
      }
    },
    "feedItem": {
      "type": "object",
      "required": ["subject", "view"],
      "description": "One record selected for the feed together with its hydrated feed-specific representation.",
      "properties": {
        "subject": {
          "type": "string",
          "format": "at-uri",
          "description": "AT-URI of the source record represented by this entry."
        },
        "view": {
          "type": "union",
          "closed": false,
          "refs": ["org.hypercerts.feed.defs#hypercertsFeedView"],
          "description": "Hydrated representation produced by the selected feed."
        }
      }
    }
  }
}
